import React from 'react';

export const NoData = () => {
    return (
        <div>
            <p>Sorry, no items to display</p>
        </div>
    );
};

export const Loading = () => {
    return (
        <div>
            <p>Loading...</p>
        </div>
    );
};
